<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.yd.dao.YdAuditTemplateDao">
    
	<sql id="ydAuditTemplateColumns">
		a.id AS "id",
		a.business_type AS "businessType",
		a.audit_level AS "auditLevel",
		a.audit_user_name AS "auditUserName",
		a.audit_user_loginname AS "auditUserLoginname",
		a.dept AS "dept",
		a.count_level AS "countLevel",
		a.create_date AS "createDate",
		a.update_date AS "updateDate"
	</sql>


	<sql id="select_where_clause">
		<trim prefix="where" prefixOverrides="AND|OR">
			<if test="id != null">AND a.id = #{id,jdbcType=VARCHAR}</if>
			<if test="businessType != null">AND a.business_type = #{businessType,jdbcType=VARCHAR}</if>
			<if test="auditLevel != null">AND a.audit_level = #{auditLevel,jdbcType=INTEGER}</if>
			<if test="auditUserName != null">AND a.audit_user_name = #{auditUserName,jdbcType=VARCHAR}</if>
			<if test="auditUserLoginname != null">AND a.audit_user_loginname = #{auditUserLoginname,jdbcType=VARCHAR}</if>
			<if test="dept != null">AND a.dept = #{dept,jdbcType=VARCHAR}</if>
			<if test="countLevel != null">AND a.count_level = #{countLevel,jdbcType=INTEGER}</if>
			<if test="createDate != null">AND a.create_date = #{createDate,jdbcType=DATETIME}</if>
			<if test="updateDate != null">AND a.update_date = #{updateDate,jdbcType=DATETIME}</if>
		</trim>
	</sql>
	
	<sql id="ydAuditTemplateJoins">
	</sql>
    
	<select id="get" resultType="YdAuditTemplate">
		SELECT 
			<include refid="ydAuditTemplateColumns"/>
		FROM yd_audit_template a
			<include refid="select_where_clause"/>
	</select>
	
	<select id="findList" resultType="YdAuditTemplate">
		SELECT 
			<include refid="ydAuditTemplateColumns"/>
		FROM yd_audit_template a
		<include refid="ydAuditTemplateJoins"/>
		<include refid="select_where_clause"/>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="YdAuditTemplate">
		SELECT 
			<include refid="ydAuditTemplateColumns"/>
		FROM yd_audit_template a
		<include refid="ydAuditTemplateJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO yd_audit_template(
			id,
			business_type,
			audit_level,
			audit_user_name,
			audit_user_loginname,
			dept,
			count_level,
			create_date,
			update_date
		) VALUES (
			#{id},
			#{businessType},
			#{auditLevel},
			#{auditUserName},
			#{auditUserLoginname},
			#{dept},
			#{countLevel},
			#{createDate},
			#{updateDate}
		)
	</insert>
	
	<update id="update">
		UPDATE yd_audit_template SET 	
			business_type = #{businessType},
			audit_level = #{auditLevel},
			audit_user_name = #{auditUserName},
			audit_user_loginname = #{auditUserLoginname},
			dept = #{dept},
			count_level = #{countLevel},
			update_date = #{updateDate}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM yd_audit_template
		WHERE id = #{id}
	</update>
	
</mapper>